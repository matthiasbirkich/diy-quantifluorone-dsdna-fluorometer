# CircuitPython 9.1.1 for Adafruit PyBadge

Included file:

`adafruit-circuitpython-pybadge-en_US-9.1.1.uf2`

Use the PyBadge UF2 bootloader drive (`PYBADGEBOOT` or `BADGEBOOT`) for
installation. Copy the UF2 file to the boot drive and wait for the board to
restart as `CIRCUITPY`.
